Al enviar una petición POST a la ruta /registro con un objeto JSON, el servidor recibe los datos mediante req.body y responde con un JSON que contiene el estado "Datos recibidos", el nombre y el mensaje enviados. La prueba confirmó que la API procesa correctamente la información recibida.
